import React from "react";
import Menu from "./Menu"; // adjust the path if needed

const NavBar = () => {
  return (
    <nav className="navbar">
      <h2>My Website</h2>
      <Menu />
    </nav>
  );
};

export default NavBar;
